#pragma once
#include <Windows.h>

enum KeyboardKeys {
	Shift = VK_SHIFT,
	Ctrl = VK_CONTROL,

	Key_S = 'S',
	Key_A = 'A',

	LeftArrow = VK_LEFT,
	RightArrow = VK_RIGHT,
	UpArrow = VK_UP,
	DownArrow = VK_DOWN,
};